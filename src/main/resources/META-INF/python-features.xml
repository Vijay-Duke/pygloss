<?xml version="1.0" encoding="UTF-8"?>
<idea-plugin>
    <!-- Optional PythonCore sub-descriptor: no <description> here — it has no effect in a
         config-file sub-descriptor and the platform logs a PluginException at load if present. -->
</idea-plugin>
